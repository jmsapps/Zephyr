{
  "name": "zephyr-basic-example",
  "version": "1.0.0",
  "main": "app.js",
  "license": "MIT",
  "scripts": {
    "dev": "esbuild app.ts --bundle --outfile=app.js --watch --servedir=.",
    "dev-jsx": "esbuild app.jsx --bundle --outfile=app.js --watch --servedir=."
  },
  "devDependencies": {
    "esbuild": "^0.25.8",
    "eslint": "^9.33.0"
  }
}
